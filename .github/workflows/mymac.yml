name: My Mac Workflow

on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:14
    steps:
      - run: npm --version
      - run: node --version
      - uses: actions/checkout@v2
      - run: node app.js